# Observed in production

**Поставка:** 2026-09-01-bootstrap-delivery-contour
**Наблюдали:** 2026-09-01 → 2026-09-01 (контур процесса; «прод» здесь — сам репозиторий)
**Сигнал из STATUS:** delivery_check зелёный на чистом клоне + чеклист §7.3 закрыт целиком + следующая поставка (ядро, класс L) стартует с непустым spec из DESIGN.md

## Примеры спеки против прода

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| A4 | `delivery_check` запускается и выходит без errors | `python3 scripts/delivery_check.py` → `0 error(s), 1 warning(s)`, exit 0 | ПОДТВЕРЖДЁН |
| A5 | `.claude/settings.json` совпадает с блоком `agent-permissions` | сверяется тем же прогоном, расхождений нет | ПОДТВЕРЖДЁН |
| A6 | `.gitkeep` закоммичен | `git ls-files delivery/active/.gitkeep` → непусто | ПОДТВЕРЖДЁН |
| A9 | публичный репозиторий, main запушен, секретов нет | github.com/AnthonyPriceOne2691/g-connect, PUBLIC; грепы по паттернам токенов пусты | ПОДТВЕРЖДЁН |

## Метрика: стало ли хуже

| Метрика | До мержа | После | Порог | Вердикт |
|---|---|---|---|---|
| errors у `delivery_check` | 2 (на первом прогоне) | 0 | 0 | В пределах |

## Что сделано по результату

- [x] Расхождений нет — наблюдение закрыто
- [ ] Расхождение → новая поставка `kind: bugfix`
- [ ] Деградация выше порога → откат

**Урок для `archive/INDEX.md`:** L1 — на пустом проекте гейт красит не то, чего нет, а то, что объявлено и не сделано: `delivery_check` поймал объявленный в constitution блок `agent-permissions` без `.claude/settings.json` и решение без цены. То есть первый прогон в свежем репозитории проверяет **согласованность заявлений**, а не код, и именно поэтому он не бесполезен до появления кода.
