# Archive index

Что это: адресный указатель по `archive/`. Перед implement найди строки, чьи пути
пересекаются с твоим диффом, прочитай их уроки и сошлись на id в `plan.md`
(M/L) или `tasks.md` (S). Не применимо — напиши одной строкой почему.

Правила ведения:
- Строка добавляется на handoff, вместе с переносом поставки в `archive/`.
- Пути — префиксы от repo-root, в backticks. Только те, где жил урок.
- Урок — что мы **узнали о системе**, а не что сделали. «Добавил кэш» — не урок.
  «Пустой robots.txt и недоступный robots.txt — разные случаи, мы их слили» — урок.
- Урок без id не считается: id — то, чем на него ссылаются.
- Поставка без урока — строка с `—` в колонке. Это законно и означает «ничего
  нового не узнали»; частое `—` подряд означает, что разборы стали формальными.

| Дата | Поставка (каталог) | Пути | Урок |
|---|---|---|---|
| 2026-09-02 | `2026-09-02-mcp-phase-2` | `src/mcp/`, `src/core/sheets/rows.ts`, `src/core/undo.ts`, `src/policy/`, `tests/`, `README.md`, `.cursor/` | **L5:** пустой или нулевой результат, который ядро не называет само, называет слой выше — и врёт: `dryRun:false` на «значение уже такое» возвращал `ok` без строки журнала («готово» при пустом факте), а `nothing_to_undo` без причины превратился в «журнал по этой цели пуст» при четырёх откаченных записях. Оба нашлись только тогда, когда ответ прочитал ЧЕЛОВЕК через клиента: проба через SDK видит протокол, а не то, что поймёт человек. **L6:** обещание, которое инструмент печатает в ответе, обязан исполнять механизм, а не память модели — `gc_targets` отдавал «лист по умолчанию: Проекты», а операции без явного `sheet` уходили на первый лист книги («Инструкция»), и от записи в чужой лист спасло лишь отсутствие там нужной колонки. **L7:** тест может закреплять дефект: property-тест «upsert идемпотентен» требовал `ok` от записи, которая ничего не меняет, — то есть охранял ровно L5; оракул проверять обратным прогоном «краснеет ли он без правки». **L8:** id примеров не должны совпадать с нотацией домена — в проекте про таблицы `B2`/`B5` это ещё и адреса ячеек, поэтому §3.1d нашла их в фикстурах (`=B2*1`, `Лист1!B5`) и объявила ожидание придуманным после кода; переименовывать id нельзя (докстринг проверки: так уже рвали связь с подписанными заказчиком id) — учить проверку требовать id в позиции ячейки таблицы |
| 2026-09-01 | `2026-09-01-core-phase-1` | `src/core/`, `src/cli/`, `scripts/lint/`, `.pre-commit-config.yaml`, `.github/workflows/` | **L2:** работа прямо в `main` обнуляет `delivery_metrics` и оставляет `merge_guard.sh` без единого суждения — гейт мержа лежит в репозитории и не работает; ветка обязательна с фазы 2. **L3:** живое чтение проверяет только то, что есть в конкретном файле: дата, приходящая серийным числом, не всплыла, потому что в той таблице не было колонки с датами — шов с библиотекой нужен отдельным тестом на ответ, собранный по документации. **L4:** дизайн-док опережает код молча: §9.3 про «две таблицы на листе» был написан и не реализован, и нашёл это СКРИНШОТ от человека, а не тесты — пунктам дизайна нужен оракул «реализовано ли», иначе документ врёт в сторону оптимизма |
| 2026-09-01 | `2026-09-01-bootstrap-delivery-contour` | `delivery/`, `scripts/`, `AGENTS.md`, `.claude/settings.json` | **L1:** первый прогон гейтов в свежем репозитории проверяет согласованность **заявлений**, а не код: поймал объявленный `agent-permissions` без `.claude/settings.json` и решение без цены. Отсюда же обратное правило — на нулевом коде разворачивать CQG нельзя, там гейты просмотрят ноль файлов и выйдут зелёными |
