# Eval smoke — эта поставка

Выведено из acceptance-примеров `spec.md`. Гоняется на фазе verify.
Прогон: `npx vitest run` (A1–A12 как тесты) + живая проверка на таблице владельца.

## Примеры спеки как оракулы (на моках, `tests/sheets.test.ts`)

- [x] A1 — карта листа: шапка в строке 2, `usedRange` A1:D4, типы, enum из `dataValidation`, 2 строки данных
- [x] A2 — upsert по существующему ключу: одна правка `D3` (2 → 3), строк по-прежнему 2
- [x] A3 — upsert нового ключа: строка 5 по колонкам
- [x] A4 — «Статус проекта» → «Статус» с оговоркой в `assumptions` (ступень 4)
- [x] A4b — алиас реестра «Затрачено» → «Часы» молча (ступень 3)
- [x] A4c — опечатка «Стаус» → «Статус» с оговоркой
- [x] A5 — «Дата» при двух датах → `needs_clarification` с кандидатами, ноль изменений
- [x] A6 — «Бюджет» → вопрос с полным списком колонок, колонка не создана
- [x] A7 — «В РАБОТЕ» → «в работе» молча
- [x] A8 — «почти готово» → вопрос со списком из проверки данных
- [x] A9 — «3ч» → число 3; «много» → вопрос
- [x] A10 — формульная колонка блокируется; с `force` разрешена
- [x] A11 — «Gconnect» → вопрос «создать или имелось в виду G connect»
- [x] A12 — нет профиля → `no_profile` + `action: connect_google`, не ENOENT

## Живая проверка (таблица владельца «Testing table infra»)

- [x] Карта листа «Проекты»: шапка в строке 2, 19 колонок, enum'ы из таблицы, дата `iso`, ссылка `link`
- [x] Превью 7 ячеек → `A4 B4 F4 G4 H4 L4 R4`, ничего не записано
- [x] Запись `--write`: `status: ok`
- [x] Идемпотентность: повторный upsert теми же значениями → «изменений нет», дубликата нет
- [x] Ступени 3 и 4 на живых колонках: алиасы молча, «Приорите» → «Приоритет» с оговоркой
- [x] Оба вида вопросов: значение вне enum и отсутствующая колонка — со списками из самой таблицы
- [x] Лист «Инструкция»: два блока, объединения в шапке и в данных, строки над шапкой — названы

## Контурные оракулы

- [x] `bash delivery/evals/smoke/run.sh` — S1–S9 зелёные
- [x] `contour_doctor.py` — DEAD 0
- [x] CI `quality.yml` — 8 шагов зелёные в чистом клоне

## Что этот сьют НЕ проверяет (названо, а не замолчано)

- Обмен кода на токен с `accounts.google.com` — существующий токен уже покрывал scopes MVP.
- Мутационную стойкость тестов — Stryker не установлен (роль частично держат 18 property-оракулов).
- Docs-примитивы и `gc_scan` — вне scope фазы 1.
