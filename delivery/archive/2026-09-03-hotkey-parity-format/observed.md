# Observed in production

**Поставка:** 2026-09-03-hotkey-parity-format
**Наблюдали:** 2026-09-03 → 2026-09-03 («прод» — живая таблица владельца, артефакт-оракул и второй агент)
**Сигнал из STATUS:** заполнен на handoff

## Примеры спеки против прода

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| V1 | вид виден в карте | у всех 19 колонок `headerFormat` = `{bold, align: center, background: #17365d}` — шапка оформлена целиком; поля `format` (тело) нет ни у одной | ПОДТВЕРЖДЁН частично: «нет вида — нет поля» видно, «вид появляется» на этой таблице проверить нечем |
| V2 | превью правки вида с кодом плана | `{kind: "format", a1: "Проекты!F4", before == after, afterFormat: {bold: true}, planId: "61f413"}` | ПОДТВЕРЖДЁН |
| V5 | частичная правка не сбрасывает остальное | у ячейки шапки `afterFormat` = `beforeFormat` + `italic`: жирность, выравнивание и фон сохранены | ПОДТВЕРЖДЁН |
| V8 | превью замены перечисляет ячейки | `changes: [{a1: "Проекты!F4", before: "На приёмке", after: "Запущено"}]`, `planId: "0dcc24"` | ПОДТВЕРЖДЁН |
| V10 | битое выражение → отказ до записи | `find: "["`, `searchByRegex: true`, `dryRun: false` → `bad_regex`, ни одной правки | ПОДТВЕРЖДЁН |
| V11 | неизвестное поле вида → отказ со списком | `{fontSize: 14}` → `bad_request` + «Допустимые поля вида: bold, italic, underline, align, background» | ПОДТВЕРЖДЁН |
| V3, V4, V6 | запись по коду, журнал, откат вида | тестами и обратным прогоном; в проде НЕ воспроизводили | ПОДТВЕРЖДЕНЫ тестами, не продом |
| V12 | правка без изменений → `no_change` | в проде дал `preview` — ДЕФЕКТ, найден прогоном и починен | РАСХОЖДЕНИЕ → починено в этой же поставке |

## Метрика: стало ли хуже

| Метрика | До поставки | После | Порог | Вердикт |
|---|---|---|---|---|
| `npx vitest run` | 0.95 с (13 файлов, 249 тестов) | 1.1 с (14 файлов, 280 тестов) | под 2 с | В пределах |
| Мутационный счёт | 63.06% | **62.05%** | не ниже 50 | В пределах, но НИЖЕ входа |
| Оракулов в smoke | 11 | 11 | все исполняются | Без изменений |
| Правил политики | 24 | 26 | у каждого проба | Растут вместе с механизмом |
| Запросов к Google на правку вида | — | 1 чтение + 1 batchUpdate | цена принята в спеке | Как задумано |

Мутационный счёт ниже входа на процент — назвал в verify-report, а не сгладил: фаза добавила
поверхности быстрее, чем оракулов. После четырнадцати целевых оракулов слабые места
сместились с нового кода (`plan.ts` 57.5 → 72.5, `find-replace-op.ts` 52.1 → 65.6) на старый
`map.ts` (57.4%), до которого в этой фазе не доходили руки.

## Что сделано по результату

- [x] Расхождения найдены и закрыты В ЭТОЙ ЖЕ поставке (блокер + три дефекта)
- [ ] Расхождение → новая поставка `kind: bugfix`
- [ ] Деградация выше порога → откат

**Что прод показал неожиданно.** Живой прогон вторым агентом нашёл блокер, которого не видел
ни один из 266 тестов: поле, которое хеш сериализовал, никто не заполнял. Дыра легла между
двумя наборами оракулов — тестом на хеш от рукописного плана и тестами операции, сравнивавшими
планы по ячейкам, но не по кодам. Отсюда практика `delivery/LIVE-RUN.md` и роль «читатель без
прогона»: искать обещания без механизма.

**Открытый сигнал наблюдения:** пишущая половина новых операций (запись по коду → журнал →
откат) ждёт прогона, где код плана произносит человек. Плюс прежний, не про код: семидневный
предел доступа.

**Урок для `archive/INDEX.md`:** L13–L16 — см. строку индекса.
