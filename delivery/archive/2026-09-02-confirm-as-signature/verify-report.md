# Verify report

**Date:** 2026-09-02
**Verifier:** human:anton
**asserts_reviewed_by:** human:anton at=2026-09-02 (примеры B14–B21 и три закрытых вопроса подписаны до первого коммита кода)
**CI run:** `quality.yml` идёт на push в main и на pull_request; пуш ветки его не запускает — прогон в чистом клоне даёт мерж через `merge_guard.sh`
**clean_clone_run:** `merge_guard.sh` проверяет СЛИТОЕ состояние в отдельном worktree — это и есть прогон на чистом дереве
**Commit:** заполняется на приёмке

## Shape oracles

- [x] PASS — `npx tsc --noEmit` (strict, `exactOptionalPropertyTypes`), `npx eslint src tests`, `prettier --check`
- [x] PASS — 23 хука pre-commit. **Гейты отклонили две попытки коммита, и обе по делу:**
      eslint нашёл неиспользованный импорт после выноса `parseA1`; гейт длины файла и гейт
      сложности потребовали сплита (`rows.ts` 554 строки, `tools.ts` 516, `prepare` 20).
      Порог не поднимался — разделено по вопросам: `write-types.ts` (типы),
      `write-guards.ts` («пускать ли записывать»), `mcp/deps.ts`, `mcp/apply-support.ts`;
      `prepare` разложен на три функции, `finish` — на диспетчер и `commit`.
- [x] PASS — исторические адреса импортов сохранены ре-экспортом: внешние вызовы не тронуты

## Behavior oracles

- [x] PASS — `npx vitest run`: **233 теста**, 13 файлов (было 216 на входе в фазу)
- [x] PASS — **обратный прогон каждого нового оракула** (урок L7): снял предусловие — три
      покраснели; снял подтверждение и однократность — четыре покраснели; снял подстановку
      листа из реестра — два. Оракул, который не краснеет без своей правки, ничего не судит.
- [x] PASS — **мутационный гейт 63.06%** при пороге падения 50. Новый код судится:
      `write-guards.ts` **74.0%**, `a1.ts` **67.4%**, `plan.ts` **68.2%**.

### Что нашёл сам мутационный гейт

**Он не судил ни одной строки этой фазы, и был при этом зелёным.** В `stryker.config.json`
область задавалась ЯВНЫМ СПИСКОМ девяти файлов. Новые модули фазы 2.6 — `plan.ts`,
`write-guards.ts`, `a1.ts` — в список не попали, потому что список никто не обновлял:
63.09% относились к старому коду, а про подтверждение гейт молчал.

Заменено маской `src/core/**/*.ts` за вычетом адаптеров Google и модулей-данных. Теперь
новый модуль попадает под гейт сам, а не когда о нём вспомнят. Счёт после расширения
области — 62.93% против 63.00%: практически тот же, но теперь он про весь код решений.

Расширение сети сразу показало слабое место, которого раньше не было видно: **`ops.ts`
37.61%** — схема на zod, где выживает большинство мутантов. Это честная новая
непокрытость, а не регресс; поднимать — задачей следующей фазы.

## Product oracles

- [x] PASS — `bash delivery/evals/smoke/run.sh`: S1–S11 зелёные. **S11 новый** — артефакт-
      оракул `scripts/probe_mcp.mjs`: сервер стартует из `dist/`, отвечает на `initialize`,
      отдаёт шесть инструментов и ресурс правил, `gc_apply` объявляет `confirm`. В фазе 2
      поле `artifact_oracle` стояло `n/a`, потому что проба гонялась разовыми файлами.
- [x] PASS — живой режим пробы (`--live`) на таблице владельца: превью отдало код
      `f25268`, запись без кода отклонена `confirm_required`. Ни одной записи.
- [x] PASS — **прогон в Cursor живым клиентом**, сверено по журналу и таблице:
      «пиши» без кода → отказ, и **агент не подставил код из своего же превью**, хотя он был
      у него в контексте: назвал и потребовал от человека. Это проверка не механизма, а
      текста политики — и она прошла.
      «пиши f25268» → запись `Проекты!F4 'На приёмке' -> 'Запущено'`, ревизии **30 → 31**,
      в журнале `planId: f25268`, id `gc-mtkh0l3y01`.
      Откат → `undoOf: gc-mtkh0l3y01`, в записи отката тот же `planId`, лист вернулся.

## Что закрыто из спеки

| id | Проверка | Статус |
|---|---|---|
| B14 | превью отдаёт `planId` из 6 символов, тот же код в HTML, значения «до» в плане | ✅ тест + живой прогон (`f25268` в чате и в отчёте) |
| B15 | `dryRun:false` без `confirm` → отказ `write.plan-confirmation`, ни одной записи | ✅ тест + проба + живой клиент |
| B16 | `confirm` от другого плана → `plan_mismatch` + НОВЫЙ план и код в ответе | ✅ тест |
| B17 | правка между превью и записью → `stale_value` с названием ячейки | ✅ тест (в проде не воспроизводили: никто не правил лист в этот момент) |
| B18 | `force` без `confirm` → отказ; с кодом плана, где отмечен `touches` → проходит | ✅ тест + проба правила |
| B19 | записаны ровно ячейки плана — сверка ЖУРНАЛА с планом | ✅ тест + живой журнал |
| B20 | повторный `confirm` на `appendRow` → `plan_already_applied`, второй строки нет | ✅ тест + проба правила; **живым клиентом не достигается**, см. ниже |
| B21 | откат несёт `planId` откаченной правки | ✅ тест + живой журнал |

## Названные слабые места (не спрятаны в зелёное)

1. **`write.plan-once` живым клиентом не достигается — и это правильно.** Второе «пиши
   f25268» дало `no_change`, а не `plan_already_applied`: после первой записи значение уже
   целевое, план пуст, и ветка нулевого изменения срабатывает раньше. Гарантия та же —
   второй записи нет, — но правило достижимо только там, где повтор ВСЁ ЕЩЁ меняет лист,
   то есть у `appendRow`, ради которого оно и написано. Порядок веток закреплён отдельным
   оракулом, чтобы не выглядел случайным.
2. **Смысловая ошибка внутри разрешённого не ловится.** Правильная цель, существующая
   колонка, значение из списка, ключ совпал — но проект не тот. Средства против этого два и
   оба прежние: человек, читающий план, и `gc_undo`. Фаза сделала их надёжными, а не
   заменила.
3. **Проза модели остаётся её прозой.** Код плана закрывает, ЧТО запишется, но не ЗАЧЕМ. В
   живом прогоне агент дважды объяснил результат своими словами точнее, чем ядро
   («код уже использован» вместо «значения уже такие») — на этот раз в верную сторону.
4. **Стойкость кода — к невнимательности, не к подбору.** Шесть hex; вычислительно
   враждебный клиент не предполагается, и это допущение записано в спеке.
5. **`ops.ts` 37.61% на мутационном гейте** — самое слабое место расширенной области.

## Исполнение рисковых путей (§12.6)

`runtime_paths: src/core/sheets/rows.ts`

- **Исполнен 2026-09-02.** Пишущий путь пройден живым клиентом целиком: отказ без кода,
  запись по коду, повтор, откат. Отказ этого пути тестами не воспроизводится — он выглядит
  как «человек сказал „пиши“ про один план, а записался другой», и увидеть это можно только
  в живом диалоге.

`model_surface: src/policy/rules.md, src/policy/rules.json`

- **Изменена в этой поставке**: правил стало 24 (+4 про подтверждение), в `rules.md` пять
  новых пунктов, включая запрет придумывать код плана. Биекция «правило ↔ проба» судится
  тестом: правило без пробы добавить нельзя. Живой прогон подтвердил, что текст доходит —
  агент отказался подставлять код сам.

## Harness metrics (this shipment)

<!-- generated by scripts/delivery_metrics.py --base origin/main -->

| Metric | Value |
|---|---|
| files_touched / loc_diff | 26 code (+7 process docs) / +1424/-321 (net +1103) |
| commits | 5 |
| time_to_accepted_spec | 0.5h |
| rework_after_done | 0 (handoff not declared yet) |
| harness_hardened | yes — delivery/evals/smoke/run.sh, tests/plan.test.ts (новый оракул) |
| implement_retries | MANUAL — fills from session log |
| verify_fails_before_green | MANUAL — count red verify runs (CI run list) |
| est_token_or_cost | MANUAL / n/a |

MANUAL-поля заполняет агент/человек на handoff. Если `verify_fails_before_green >= 2` при `harness_hardened: no` — по §9.2 добавь oracle/breaker/hook в этой же поставке.

## Инцидент: сьют снёс рабочий профиль владельца

**Что случилось.** Мутационный прогон 22:24 записал тестовые фикстуры в РЕАЛЬНЫЙ
`~/.gconnect/default/`: `token.json` (`access_token: "a"`, `refresh_token: "r"`) и
`credentials.json` (клиент `id.apps.googleusercontent.com`, секрет из шести символов).
Доступ к Google у владельца умер: и CLI, и MCP-путь стали отвечать `auth_expired`.
`targets.json`, журнал и отчёты целы.

**Причина — правка этой поставки.** Я расширил область мутаций с явного списка на маску
`src/core/**`, и под мутацию попал `env.ts` — единственный модуль, который отводит запись
профилей в временный каталог через `GCONNECT_HOME`. Мутант сломал перенаправление, а
`profiles.test.ts` пишет токен и креды для аккаунта `default` — и записал их в рабочий
профиль. Дыра была не в мутанте: **сьют в принципе мог писать в реальный профиль**, потому
что отсутствие переменной означало «пиши в домашний каталог».

**Восстановление.** Файл клиента нашёлся в трёх копиях старого движка (от 2026-09-01,
`client_id 328143141382-…`, секрет 35 символов) — восстанавливается копированием. Токен не
восстанавливается ниоткуда: нужен повторный `npm run gc -- login`. Обе операции — за
владельцем: копирование и чтение файлов с кредами агенту запрещено политикой среды.

**Закрыто тремя слоями, не одним:**

1. `vitest.config.ts` — `GCONNECT_HOME` на весь прогон указывает в свежий временный
   каталог. Изоляция перестала быть договорённостью «каждый тест ставит переменную» и стала
   свойством прогона.
2. `src/core/env.ts` — под тест-раннером отсутствие `GCONNECT_HOME` это отказ
   (`cause: test_home_not_isolated`), а не путь в `~/.gconnect`.
3. `stryker.config.json` — `env.ts` исключён из мутаций: мутант внутри модуля изоляции
   ломает и защиту из пункта 2, поэтому мутировать его нельзя.

Оба оракула проверены обратным прогоном: снял защиту — красный; снял исключение — красный
второй, который читает конфиг и требует и строку исключения, и объяснение в комментарии.
Прогон после правок к профилю не притронулся (отметки файлов не изменились).

## Ревью рисковых мест (§12.5)

**Класс риска: запись в чужие данные + деньги** (в целевой таблице есть колонка
«Стоимость/мес.», и пишущий путь этой поставки её тоже касается).

1. **Совпадение кодов плана.** `planId` — шесть hex-символов, то есть 1 к 16.7 млн на
   случайное совпадение двух РАЗНЫХ планов. Последствие в теории: человек подтвердил план A,
   записался план B. Компенсация не одна: перед записью работает предусловие D-17
   (`assertBeforeValues`) — план, чьи значения «до» не совпали с листом, отклоняется; а
   `assertPlanNotApplied` при совпадении кода с уже записанным даст ОТКАЗ, а не молчаливую
   запись. То есть режим отказа безопасный, а не тихий.
2. **`assertPlanNotApplied` смотрит последние 50 записей журнала.** План старше 50 записей
   по той же цели снова считается новым — для `appendRow` это означает возможный дубликат.
   Числа названы; расширять окно без нужды не стал: 50 записей по одной цели это недели
   работы в текущем сценарии.
3. **Строгое равенство кода.** `confirmationOutcome` сравнивает `options.confirm === code`
   без `trim` и без приведения регистра. Это сознательно: «почти тот же код» не должен
   проходить. Риск в будущем — кто-нибудь «улучшит» сравнение до нечёткого; на это есть
   оракул B16, который требует отказа на чужой код.
4. **CLI пишет мимо журнала.** `src/cli/manual.ts` вызывает `upsertRow` без `journal` и без
   `journalSource`: значит из CLI не работает ни аудит, ни `write.plan-once`. Это не регресс
   этой поставки (журнала там не было и раньше), но теперь дыра заметнее — код плана CLI
   передавать научился, а однократность не проверяет. Названо как остаток, чинится вместе с
   CLI-фасадом фазы 3.
5. **Одно лишнее чтение на запись.** Цена предусловия принята в спеке; на квоту Google
   влияет линейно от числа записей, а записи здесь единичные и человеческие.

## Изменение поверхности модели (§14.4)

`model_surface: src/policy/rules.md, src/policy/rules.json` — **изменена в этой поставке** at=2026-09-02.

- `rules.json`: **+4 правила** (стало 24) — `write.plan-confirmation`,
  `write.force-needs-plan`, `write.value-precondition`, `write.plan-once`. У каждого
  `enforced_in` на существующий файл и своя проба; биекция «правило ↔ проба» судится тестом,
  поэтому правило без пробы добавить нельзя.
- `rules.md`: **+5 пунктов** человеческим текстом, включая прямой запрет придумывать код
  плана, брать его из своего предыдущего вызова или подставлять «тот, что был».
- Почему это отдельный блок: правка одной строки здесь меняет поведение продукта при
  нетронутой реализации и зелёных гейтах. Проверено не только тестом, но и живым клиентом:
  в Cursor агент **назвал** код и потребовал его от человека, хотя код был у него в
  контексте. Текст доехал до модели и сработал.

---

**READY FOR HANDOFF** — да. Восемь примеров закрыты, пишущий путь пройден живым клиентом,
инцидент с профилем закрыт механизмом и двумя оракулами, слабые места названы: `plan-once`
недостижим живым клиентом (только `appendRow`), окно журнала 50 записей, CLI без журнала,
`ops.ts` 37.6% на мутационном гейте, смысловая ошибка внутри разрешённого механизмом не
ловится.
