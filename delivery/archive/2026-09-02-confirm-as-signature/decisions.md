# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-02 · выбрал пересчитывать код плана из фактического состояния вместо хранилища планов в `~/.gconnect/plans/` — потому что хранилище это четвёртая копия правды рядом с таблицей, журналом и снимком «до»: ей нужны TTL, ротация и уборка, а разъезжаться она будет молча. Цена замерена на соседнем каталоге: `~/.gconnect/reports/` без ротации дал за неделю сотни файлов и потребовал правила `reports.keep-last` (50). Код считает `planId` в `src/core/plan.ts` из `PlanShape`, который собирает `planOf` в `rows.ts` перед каждой записью — состояния между вызовами не хранится вовсе
- 2026-09-02 · выбрал класс M вместо L — потому что объём одна плитка (четыре механизма поверх готового `gc_apply`), но подпись плана всё равно беру: путь пишущий, а governance требует human_ok_plan на risky (§Governance конституции)
- 2026-09-02 · выбрал код плана длиной 6 символов вместо 4 — потому что защита нужна не от подбора (её тут нет), а от невнимательности: 4 символа человек набирает «по памяти похоже», 6 — уже нет. Цена в трении названа числом: два лишних символа в каждой реплике подтверждения
- 2026-09-02 · выбрал требовать `confirm` и для `appendRow` вместо освобождения журнальных дописываний — потому что у `appendRow` нет значений «до», а ревизия в хеше от повторного «пиши» не спасает: второй вызов дал бы вторую строку в таблице (пример B20). Освобождение уменьшило бы трение в самом частом сценарии и вернуло бы дыру именно туда
- 2026-09-02 · выбрал оставить `expectRevision` как контракт для вызовов без превью вместо удаления поля — потому что ревизия входит в код плана только у тех, кто превью получал; CLI и раннер фазы 4 зовут запись напрямую, и удаление отняло бы у них единственную защиту от гонки с живой правкой. Цена: два способа сказать одно и то же, поэтому поле помечается в описании инструмента
- 2026-09-02 · выбрал изолировать весь прогон тестов через `env: { GCONNECT_HOME }` в `vitest.config.ts` вместо договорённости «каждый тест ставит переменную в beforeEach» — потому что договорённость держалась до первого мутанта: прогон 22:24 записал фикстуры в реальный `~/.gconnect/default/` и снёс токен и файл OAuth-клиента владельца (`token.json` с `access_token: "a"`, `credentials.json` со секретом из шести символов). Цена: одна строка конфига и свежий временный каталог на прогон
- 2026-09-02 · выбрал сделать отсутствие `GCONNECT_HOME` под тест-раннером ОТКАЗОМ в `src/core/env.ts` вместо тихого падения в домашний каталог — потому что тест без изоляции должен быть громко красным, а не разрушительным; `cause: test_home_not_isolated` называет причину прямо. Цена: новый тест, забывший переменную, упадёт — это и есть цель
- 2026-09-02 · выбрал исключить `src/core/env.ts` из области мутаций вместо оставления его под гейтом — потому что мутант внутри модуля изоляции ломает и саму защиту (`read` мутируется вместе с ней), то есть сьют превращается в разрушительный инструмент. Цена названа числом: минус 12 мутантов из области (env.ts давал 63.16%), и на молчаливое возвращение файла в область есть оракул, читающий `stryker.config.json`
