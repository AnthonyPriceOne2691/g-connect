# Observed in production

**Поставка:** 2026-09-02-mcp-phase-2
**Наблюдали:** 2026-09-02 → 2026-09-02 («прод» здесь — живая таблица владельца и два разных MCP-клиента)
**Сигнал из STATUS:** B1–B13 подтверждены в двух клиентах (Claude Code и Cursor), включая запись и откат живым клиентом; smoke S1–S10 зелёный; `delivery_check` 0/0; мутационный счёт не ниже порога 50

## Примеры спеки против прода

| id | Обещано в спеке | Что в проде | Вердикт |
|---|---|---|---|
| B1 | ровно шесть инструментов со схемами | `tools/list` через протокол: `gc_targets`, `gc_read`, `gc_search`, `gc_scan`, `gc_apply`, `gc_undo`; `instructions` 4887 символов | ПОДТВЕРЖДЁН |
| B2 | права без секретов и путей к профилю | `gc_targets`: `infra-test`, `allow: write`, `sheet: Проекты`, `headerRow: 2`; ни токенов, ни `.gconnect` в ответе | ПОДТВЕРЖДЁН |
| B3 | карта с предупреждениями | лист «Проекты»: шапка 2, 19 колонок, enum «Статуса» из проверки данных, строка над шапкой в `warnings` | ПОДТВЕРЖДЁН |
| B4 | без `dryRun:false` — превью, ни одной записи | превью `F4: На приёмке → Запущено`; в журнале за это время строки нет | ПОДТВЕРЖДЁН |
| B5 | неизвестная колонка → вопрос со списком всех колонок | проба на листе «Инструкция»: `available` = все 4 колонки листа. **Оговорка:** в чате Cursor агент пересказал вопрос одной ближайшей колонкой («Стоимость/мес.»), полный список остался в сыром ответе — механизм честен, пересказ беднее | ПОДТВЕРЖДЁН |
| B6/B6b/B7 | мусор и запись вне реестра отклоняются до сети | `op: "удалиВсё"` → протокольная `-32602`; цель вне реестра → `policy_denied` / `write.allowlist` без обращения к Google | ПОДТВЕРЖДЁН |
| B8 | `policy://rules` + `instructions` из `rules.md` | ресурс прочитан клиентом, 20 машинных правил в `instructions` | ПОДТВЕРЖДЁН |
| B9 | запись → откат вернул значения, две записи в журнале | `upsertRow` id `gc-mtk11gt301`, ревизии 25 → 26; `undo` с `undoOf: gc-mtk11gt301`, обратная запись `'Запущено' -> 'На приёмке'` | ПОДТВЕРЖДЁН |
| B10 | ревизия сдвинулась → `revision_conflict` | тест с изменённой ревизией; в проде не воспроизводили (никто не правил лист во время прогона) | ПОДТВЕРЖДЁН тестом, не продом |
| B11 | строка журнала с ревизиями до/после, права 600 | 6 строк в `2026-09.jsonl`, права `-rw-------`, в строках время, профиль, цель, лист, ячейки, ревизии | ПОДТВЕРЖДЁН |
| B12 | HTML-превью «было → станет», самодостаточный | `2026-09-02T11-41-56-preview.html` (2485 Б) и `…11-44-39…` (2583 Б), права 600 | ПОДТВЕРЖДЁН |
| B13 | одна формулировка в двух клиентах → один результат | Claude Code и Cursor: один и тот же сценарий, одинаковые статусы и одинаковые записи в журнале | ПОДТВЕРЖДЁН |

Дополнительно подтверждено в проде (появилось из разбора этого же прогона): `no_change`
без строки журнала на повторной правке; `all_undone` с числом 6, совпадающим с журналом;
лист берётся из реестра, а не первый в книге; `gc_read values` не отдаёт пустоту за
границей данных.

## Метрика: стало ли хуже

| Метрика | До мержа | После | Порог | Вердикт |
|---|---|---|---|---|
| `npx vitest run` | 0.83 с (8 файлов, 107 тестов) | 0.89 с (12 файлов, 201 тест) | под 2 с | В пределах |
| Мутационный счёт | 58.03% | **63.09%** | не ниже 50 | В пределах, вырос |
| Уязвимости JS (рантайм) | 0 | 0 (`npm audit --omit=dev`) | 0 critical/high | В пределах |
| Уязвимости JS (dev) | 4 moderate (снимок) | 2 moderate | не выше снимка | В пределах |

## Что сделано по результату

- [x] Расхождений по примерам нет — наблюдение по B1–B13 закрыто
- [ ] Расхождение → новая поставка `kind: bugfix`
- [ ] Деградация выше порога → откат

**Наблюдение остаётся открытым по одному сигналу, и он не про код.** Приложение в Cloud
Console стоит в статусе Testing: Google обрубает refresh-токен через семь дней, ядро
предупреждает с шестого (`auth.testing-mode-warning`). Проверять нечего до 2026-09-08 —
либо предупреждение появится и вход переживётся заново, либо приложение будет
опубликовано и сигнал умрёт. `observe_until: 2026-10-15` держится ради этого.

**Урок для `archive/INDEX.md`:** L5, L6, L7 — см. строку индекса.
