# Verify report

**Date:** 2026-09-01
**Verifier:** human:anton
**asserts_reviewed_by:** deferred — acceptance-примеры A1–A12 в spec.md написаны до кода, но человеком пока не подписаны (`human_ok_spec: deferred`, §2.2b); на handoff требуется подпись
**CI run:** https://github.com/AnthonyPriceOne2691/g-connect/actions/runs/33531442569 (зелёный целиком)
**clean_clone_run:** npm ci + tsc --noEmit + vitest run + delivery_check в чистом клоне на ubuntu-latest / Node 24 — все четыре шага зелёные at=2026-09-01
**Commit:** 950d2b8

## Shape oracles
- [x] PASS — `npx tsc --noEmit` (strict, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`) в чистом клоне
- [ ] n/a — CQG не развёрнут (`shape-oracles: weak`), момент — конец фазы 1

## Behavior oracles
- [x] PASS — `npx vitest run`: 81 тест, 6 файлов, зелено локально и в чистом клоне
- [x] PASS — реляционные оракулы (§6.5, `tests/properties.test.ts`, fast-check), 18 свойств. Ключевое: **идемпотентность upsert** на изменяемой фикстуре (второй прогон не даёт изменений, строк остаётся столько же), «в плане не бывает before === after», «dryRun не пишет при любых значениях», «любое имя колонки в любом регистре резолвится молча», «при вопросе колонка не выбрана», «значение enum приводится к каноническому». Плюс инвариант «любая ссылка вне реестра — только чтение», round-trip ID→URL→ID по всем формам ссылок, идемпотентность резолва, «неповторяемая ошибка = ровно один вызов при любом бюджете», монотонность backoff, round-trip токена с правами 600 при любом имени аккаунта, «состояние профиля определено при любой комбинации файлов»

## Product oracles
- [x] PASS — оракул S1 (`delivery_check`) исполняется в CI, а не только локально
- [x] PASS — шаг «Delivery contour» зелёный после компенсации §10.4 п.4; открытый вопрос
      (waiver или гейт мержа) остаётся в `active/escalation.md` и ждёт владельца

## Что закрыто из спеки

| id | Проверка | Статус |
|---|---|---|
| A12 | нет профиля → `no_profile` + `action: connect_google`, а не ENOENT | ✅ `tests/profiles.test.ts` |
| — | 401 `invalid_grant` и 403 про scope дают **разные** действия | ✅ `tests/errors.test.ts` |
| — | произвольная ссылка не становится пишущей целью (§11.2) | ✅ `tests/targets.test.ts` |
| — | токен 600 в каталоге 700, статус без секретов (§13.5) | ✅ `tests/profiles.test.ts` |
| A1 | шапка найдена во 2-й строке, типы, enum из `dataValidation`, предупреждение про строки над шапкой | ✅ `tests/sheets.test.ts` |
| A2 | существующий ключ → одна правка `D3`, строк по-прежнему 2 | ✅ |
| A3 | нового ключа нет → строка 5 по колонкам | ✅ |
| A4 | «Статус проекта» → «Статус» с оговоркой в `assumptions` (ступень 4) | ✅ |
| A4b | подтверждённый алиас «Затрачено» → «Часы» молча (ступень 3) | ✅ |
| A4c | опечатка «Стаус» → «Статус» с оговоркой | ✅ |
| A5 | «Дата» при двух датах → вопрос с кандидатами, ноль изменений | ✅ |
| A6 | «Бюджет» → вопрос с полным списком, колонка не создана | ✅ |
| A7 | «В РАБОТЕ» → «в работе» молча | ✅ |
| A8 | «почти готово» → вопрос со списком из проверки данных | ✅ |
| A9 | «3ч» → число 3; «много» → вопрос | ✅ |
| A10 | формульная колонка блокируется; с `force` разрешена | ✅ |
| A11 | «Gconnect» → вопрос «создать или имелось в виду G connect» | ✅ |
| D-10 | в коде ядра нет имён листов и колонок (грепом, без комментариев) | ✅ |

## Исполнение рисковых путей (§12.6)

`runtime_paths: src/core/auth.ts,src/core/profiles.ts`

- `src/core/profiles.ts` — **исполнен**: чтение/запись профиля и права 600/700 проверены на реальной файловой системе во временном каталоге (`GCONNECT_HOME`), 11 тестов + property на любые имена аккаунтов. Плюс прогон на **настоящем** профиле `~/.gconnect/default/` с перенесёнными кредами: `state: connected`, 5 scopes, срок токена — прочитано верно, секреты в вывод не попали. Не исполнено: поведение при `EACCES` на чужом файле — воспроизводится только вручную, вынесено в слайс 3. at=2026-09-01
- `src/core/auth.ts` — **не исполнен**: модуля ещё нет (слайс 3). OAuth-вход, redirect-порт 3333 и обновление токена проверяются человеком на живом аккаунте; тестами это не воспроизводится, поэтому и объявлено в `runtime_paths`.

## Ревью рисковых мест (§12.5)

**Класс риска: интеграция.**

1. **`fromGoogleError` — единственная точка разбора чужих ошибок.** Форма ошибки у
   `googleapis` не стабильна между версиями (`code` числом, `code` строкой, `response.status`),
   и промах даст `internal` вместо действия. Смягчение: разбор покрыт тестами на все три
   формы; неизвестное честно падает в `internal`, а не притворяется чем-то конкретным.
2. **`resolveTarget` решает, можно ли писать.** Ошибка здесь — не косметика: она открывает
   запись в чужой документ. Смягчение: по умолчанию `read`, `write` только из реестра,
   отдельный тест на «та же ссылка без реестра — read».
3. **`allowImportingTsExtensions` + импорты с `.ts`.** Работает при `noEmit` и в Node 24, но
   в фазе 2 появится сборка `dist/` — тогда расширения придётся менять либо ставить бандлер.
   Риск назван сейчас, чтобы не всплыл как «внезапно не собирается»: решение — в плане фазы 2.

## Что нашли собственные тесты (не ревью)

1. **Off-by-one в карте листа:** правило `dataValidation` проверялось на строке шапки, а
   объявлено на данных — enum из таблицы терялся молча, и колонка «Статус» вела себя как
   свободный текст. Поймали три упавших примера сразу (A1, A7, A8).
2. **`3ч.` не разбиралось в число:** хвост срезался регуляркой `[^\d.-]+$`, а точка входила
   в разрешённый набор. Нашёл property-тест на «число остаётся числом» — контрпример
   `[0, "ч."]`. Табличные тесты этого не видели, потому что автор такой формы не придумал.
3. **Пример A4 в спеке был несогласован с дизайном:** «Затрачено» → «Часы» это ступень 3
   (подтверждённый алиас), а не ступень 4 (близость) — у слов нет общих букв. Поправлена
   спека, а не порог резолвера: подгонка означала бы, что «Бюджет» тоже «похож» на «Часы».

## Spec coverage gaps
- Слайс 3 (живой OAuth, реальный `googleapis`) не начат — фаза не закрыта, это ожидаемо.
- `human_ok_spec` и `human_ok_plan` — `deferred`: спека и план написаны, ждут прочтения владельцем.

## Verdict
- [ ] READY FOR HANDOFF
- [x] NEED CONVERGE (new tasks) — слайсы 2 и 3 по `tasks.md`
- [ ] BLOCKED
