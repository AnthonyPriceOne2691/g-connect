# Tasks

Уроки из архива (§2.2a), пересекающиеся с диффом этой поставки:

- **L2** (`src/`, workflows): работать в ветке `feat/phase-2-mcp`, мержить через
  `merge_guard.sh` — иначе гейт мержа снова не судит ничего. Применено: см. `worktree` в STATUS.
- **L3** (`src/core/google/`): шов с библиотекой проверять тестом на ответ, собранный по
  документации, а не «мы же подключились». Применимо к схемам MCP-SDK так же, как к googleapis.
- **L4** (`DESIGN.md`): пункт дизайна без оракула молча опережает код. Применено: каждый
  пункт `In scope` спеки имеет пример B1–B13, а не только описание.

## Slice 1 — граница и правила

- [x] T1: `src/core/ops.ts` — zod-схема операций (`appendRow`/`upsertRow`/`setCells` + общие поля)
- [x] T2: `src/policy/rules.md` — политика человеческим текстом
- [x] T3: `src/policy/rules.json` — машинная часть (лимиты, запреты, что требует подтверждения)
- [x] T4: исполнение правил в core + тест «каждое ограничение имеет отказ» → B6, B7
- [x] T5: тесты слайса 1

## Slice 2 — журнал и откат

- [x] T6: `src/core/audit.ts` — `~/.gconnect/audit/YYYY-MM.jsonl`, без секретов → B11
- [x] T7: `src/core/undo.ts` — снимок «до», обратная запись, сверка ревизии → B9, B10
- [x] T8: реляционный оракул «undo возвращает лист к прежнему состоянию»

## Slice 3 — сервер

- [x] T9: `@modelcontextprotocol/sdk`, `src/mcp/server.ts`, stdio
- [x] T10: шесть инструментов; JSON-схемы генерируются из zod, а не пишутся вторым текстом
- [x] T11: ресурс `policy://rules` + `instructions` собирается из `rules.md` → B8
- [x] T12: единый перехват ошибок на границе: `GcError` → payload, не стектрейс
- [x] T13: сборка `dist/`; снять `allowImportingTsExtensions` (риск из фазы 1)
- [x] T14: тесты B1, B2, B8, B12

## Slice 4 — живая проверка и передача

- [x] T15: `src/core/report/html.ts` — превью «было → станет» файлом → B12
- [x] T16: подключение к Cursor вторым клиентом к Claude Code, прогон B13 одной формулировкой
      (прогон 2026-09-02 состоялся и дал пять дефектов)
- [ ] T19: перепрогон шагов 3, 4, 7 в Cursor после правок — запись и откат ещё не проверялись
      живым клиентом ни разу
- [x] T17: Stryker — закрыть роль «тесты действительно утверждают» (остаток фазы 1)
- [ ] T18: `verify-report.md`, метрики, мерж через `merge_guard.sh`
