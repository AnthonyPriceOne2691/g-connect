# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-01 · выбрал вести фазу 2 в ветке `feat/phase-2-mcp` вместо работы в main — потому что урок L2 фазы 1 замерен: 13 коммитов в main дали `delivery_metrics` нули и `merge_guard.sh` ни одного суждения, то есть гейт мержа лежал в репозитории мёртвым
- 2026-09-01 · выбрал генерировать JSON-схемы инструментов ИЗ zod вместо второго текста рядом — потому что два описания одного контракта расходятся молча, и расхождение видно только клиенту в чужой сессии
- 2026-09-01 · выбрал сделать правила и аудит ДО сервера вместо «сначала подключим, потом ограничим» — потому что иначе первые записи пройдут мимо журнала, и «кто что записал» восстановить будет нечем
- 2026-09-01 · выбрал оставить строгую zod-схему у `op` вместо `z.string()` с ручной проверкой — потому что схема уходит в `tools/list`, и агент видит три допустимых значения ДО вызова: предотвращённая ошибка дешевле красиво оформленной. Цена названа в B6: такие ошибки приходят протокольными, а не типизированным payload, и на них не будет `action`
- 2026-09-01 · выбрал проверять право на запись ДО чтения из Google вместо порядка «прочитали, потом проверили» — потому что живая проба показала цену обратного порядка: запрос на запись в чужой файл давал «файл не найден» вместо «нет права записи», то есть лишний запрос в сеть и вводящий в заблуждение текст. Юнит-тесты этого не видели, потому что вызывали обработчик напрямую
- 2026-09-01 · выбрал писать HTML-превью по флагу `report: true` вместо записи на каждый вызов — потому что превью на каждый вызов копит сотни файлов в `~/.gconnect/reports` за месяц работы, а читают их единицы; отсутствие ротации названо остатком в verify-report
- 2026-09-01 · выбрал самодостаточный HTML без единой внешней ссылки вместо шаблона с CDN — потому что отчёт открывают с диска и иногда без сети, а внешний ресурс превратил бы «покажи, что изменится» в «загрузка не удалась»; на это есть тест
- 2026-09-01 · выбрал снять снимок deps-audit вместо waiver'а человека — потому что обе уязвимости moderate и пришли транзитивно из самого Stryker (`typed-rest-client` → `qs`), это dev-зависимость вне рантайма сервера; снимок фиксирует ровно 4 известных и продолжает краснеть на РОСТЕ и на любых critical/high (HARD_FAIL_SEVERITY=1), то есть механизм не ослаблен
- 2026-09-01 · выбрал порог мутационного гейта 50 с объявлением его правилом вместо «поставим 80 и посмотрим» — потому что фактический счёт 58.03%, и порог выше текущего сделал бы гейт красным с первого дня, а красный по построению перестают читать; правило и конфиг сверяются тестом, поэтому понизить молча нельзя, а поднять — задача следующей фазы
- 2026-09-01 · выбрал добавить оракул S10 «политика в dist равна политике в src» вместо просто исправления `cp` — потому что поломка была молчаливой: сборка проходила, сервер отдавал 14 правил из 17, и заметить это можно было только сравнив числа. Исправление без оракула означало бы «этот раз починили»
- 2026-09-01 · выбрал ограничить откат свежими записями (60 мин) вместо «откатывай последнюю неоткаченную» — потому что на живой таблице два «откати» подряд вернули не только мою правку, но и запись ЧУЖОЙ сессии сорокаминутной давности: формально последняя неоткаченная, по сути потеря чужой работы. Старую откатить можно, но назвав её id — осознанно, а не по инерции
- 2026-09-01 · выбрал перевести три копии старого движка на личный OAuth-клиент вместо сохранения корпоративного клиента — потому что так решил владелец: связка не должна зависеть от инфраструктуры организации. Цена названа: скриптам нужен новый вход, а корпоративные таблицы (redib texts Main и подобные) станут им недоступны, пока не расшарены на личный аккаунт
- 2026-09-02 · выбрал предупреждать о семидневном пределе за день вместо реакции на отказ — потому что приложение стоит в статусе Testing, и через неделю доступ отвалится посреди работы: «перестало работать без причины» дороже одной строки предупреждения. Отметку времени входа ставим сами, Google её не отдаёт
- 2026-09-02 · выбрал ротацию отчётов по числу файлов вместо ротации по возрасту — потому что число проверяемо одним `readdir` и не зависит от часов машины; возраст потребовал бы разбора имён или stat на каждый файл, а решаемая задача — «каталог не растёт бесконечно», а не «хранить ровно месяц»
- 2026-09-02 · выбрал закрывать B13 парой **Cursor + Claude Code** вместо пары Cursor + Claude Desktop — потому что Claude Desktop на машине владельца не установлен (нет ни приложения, ни `claude_desktop_config.json`), а утверждает B13 независимость от MCP-хоста, и её доказывают любые два разных хоста. Цена названа: снипет для Claude Desktop в README остаётся непроверенным, и первый его запуск — потенциальный источник дефекта, как им оказалась строка про `.mcp.json` для Cursor
