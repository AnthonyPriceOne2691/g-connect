# Decisions

Одна строка на выбор. Три части обязательны: **что выбрано**, **вместо чего**,
**потому что**. Строка без альтернативы — не решение, а отчёт о сделанном, и
`delivery_check` её отклонит.

На handoff перечитай: то, что оказалось уроком **о системе** (а не о твоём
выборе), перенеси строкой в `archive/INDEX.md` (§2.2a). Решение умирает с
поставкой, урок — нет.

- 2026-09-01 · выбрал развернуть только ① Delivery вместо полного контура ①②③④ — потому что кода ноль, а на нулевой раскладке 7 гейтов CQG из 9 просматривают ноль файлов и выходят зелёными (AGENT_STACK §2 A′ п.1): контур выглядел бы покрытым, не просмотрев ни файла
- 2026-09-01 · выбрал момент ② CQG «конец фазы 1» вместо «сразу после Delivery» — потому что baseline-ratchet имеет смысл только когда есть что мерить, а до этого его первый снимок закрепляет пустоту
- 2026-09-01 · выбрал момент ③ OKF «после фазы 2» вместо «сейчас» — потому что `okf_sync_gate` прибивает concept'ы к путям в коде через `implementation:`, а путей ещё нет; инварианты пока живут в DESIGN.md D-1…D-10 без дубля
- 2026-09-01 · выбрал класс S с `kind: bootstrap` вместо класса L — потому что объём развёртывания предписан каноном, а не проектируется; продуктовая поставка «ядро» пойдёт классом L со spec из DESIGN.md
- 2026-09-01 · выбрал публичный репозиторий на GitHub вместо приватного — потому что так попросил владелец проекта; следствие записано в constitution как non-negotiable «секреты никогда в репозиторий», креды живут в `~/.gconnect/`
- 2026-09-01 · выбрал оставить каноны вне репозитория (`Prepare/`) вместо копирования в корень — потому что каноны общие для всех проектов и версионируются отдельно; следствие (CI их не видит, `stack_selftest` в CI невозможен) не закрыто и записано в `STACK-ACCEPTANCE.md`, выбор варианта A/B/C по AGENT_STACK §7.1 — на шаге ② CQG
