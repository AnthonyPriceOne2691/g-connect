{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Мутационный гейт (CQG §3, роль «тесты действительно утверждают»). Область — модули, где живут решения: резолвер, значения, правила, журнал, откат, карта, строки. Адаптеры Google исключены: их шов проверяется тестом на ответ библиотеки, мутировать там нечего кроме маппинга.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "reporters": ["clear-text", "progress"],
  "coverageAnalysis": "perTest",
  "mutate": [
    "src/core/resolver.ts",
    "src/core/values.ts",
    "src/core/policy.ts",
    "src/core/journal.ts",
    "src/core/undo.ts",
    "src/core/targets.ts",
    "src/core/retry.ts",
    "src/core/sheets/map.ts",
    "src/core/sheets/rows.ts"
  ],
  "thresholds": { "high": 80, "low": 60, "break": 50 },
  "timeoutMS": 20000,
  "concurrency": 4
}
