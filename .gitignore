# Секреты и креды — никогда в репозиторий (профили живут в ~/.gconnect/)
credentials.json
token.json
*.token.json
.env
.env.*

# Node
node_modules/
dist/
*.tsbuildinfo

# Результаты сканов и аудит — локальные артефакты
index/
audit/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Артефакты прогонов гейтов
coverage/
.stryker-tmp/

# Отчёт мутационного гейта
/reports/

# Секрет OAuth-клиента: не должен оказаться в репозитории ни под каким именем
client_secret*.json
