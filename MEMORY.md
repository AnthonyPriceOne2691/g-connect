# MEMORY — состояние проекта

> Снапшот для новых сессий (обновлять на вехах; история — в git).
> Обновлено: **2026-09-03**. Фазы 1, 2, 2.6 и **2.5a закрыты, заархивированы и слиты в
> main** через `merge_guard.sh`; `delivery/active` пуст — штатный idle, следующая поставка
> не начата. **280 тестов**, мутационный счёт 62.05%, правил 26, smoke S0–S11 (11 из 11
> исполняются), `contour_doctor` DEAD 0, CI на main зелёный.
>
> **Следующее по порядку** (порядок задан владельцем): 2.5b — структурные операции
> (`insertRows/Columns`, `deleteRows/Columns`, `addSheet`, `copyRange/cutRange`, `autoFill`,
> `createSheet/Doc/Folder`); затем 2.7 (`gc_scan`), затем 3 (CLI и итог дня). Публикация
> приложения в Cloud Console — **в самом конце**, так решил владелец.
>
> **Первым делом, до 2.5b:** закрыть пишущую половину 2.5a живым прогоном. Запись по коду
> плана → строка журнала → откат для `formatCells` и `findReplace` тестами покрыта, живым
> клиентом нет, и агентом эта проверка невозможна (см. `delivery/LIVE-RUN.md`). Чек-лист —
> `delivery/archive/2026-09-03-hotkey-parity-format/live-run.md`. Иначе 2.5b ляжет на
> непроверенный живьём контур записи.
>
> **ВНИМАНИЕ, состояние доступа.** 2026-09-02 мутационный прогон записал тестовые фикстуры
> в реальный `~/.gconnect/default/` и снёс `token.json` и `credentials.json`. Дыра закрыта
> тремя слоями (`vitest.config.ts` задаёт `GCONNECT_HOME` на весь прогон, `env.ts`
> отказывает без него под тест-раннером, `env.ts` исключён из мутаций) и двумя оракулами.
> Восстановление: файл клиента копируется из `~/Documents/SEO:Content/Gdocs-tabs editor/`,
> токен — только `npm run gc -- login`. Если `gc status` не `connected` — начни с этого.
>
> **Мержить только через `bash scripts/merge_guard.sh <ветка> main`.** 2026-09-02 ветку
> слили обычным `merge --ff-only`, и гейт мержа не вынес ни одного суждения — урок L2
> вторым заходом. С тех пор две ветки слиты правильно, и гейт дважды блокировал мерж по
> делу (S11 без `dist` в worktree, красный smoke).
>
> **Локальный гейт мержа и CI смотрят разное:** `contour_doctor` гоняется в `quality.yml`,
> но не входит в список гейтов `merge_guard.sh`. Один раз это уже дало красный CI на main
> после зелёного мержа.
>
> **История переписана 2026-09-02 трижды** (авторство коммитов, затем два упоминания
> рабочего контура в DESIGN.md). Хеши до этой даты недействительны, коммитов стало 29,
> все подписаны `AnthonyPriceOne2691 <95498521+…@users.noreply.github.com>`.

## Что это

Универсальное ядро доступа агента к Google Docs / Sheets / Drive и локальным папкам.
Дизайн — [DESIGN.md](DESIGN.md), 16 разделов и 15 решений D-1…D-15; он источник правды,
любое изменение архитектуры = правка дизайна в том же коммите.

Два равноправных контура: **читающий** (карта файла, профиль колонок, сканирование
корпуса) и **пишущий** (превью до записи, адресация по именам колонок, вопрос вместо
догадки, откат). Мозг подключаемый: MCP-клиент, локальная модель, любой провайдер.

## Готово

**Фаза 1 — ядро.** `delivery/archive/2026-09-01-core-phase-1/`. Профили, типизированные
ошибки с действием, ретраи, резолв целей, карта листа с автоопределением шапки, профиль
колонок, резолвер на шесть ступеней, нормализация значений, `appendRow`/`upsertRow`/
`setCells`, OAuth, адаптер `googleapis`.

**Фаза 2.5a — вид, формулы, замена.** `formatCells` (bold/italic/underline/align/фон) и
`findReplace` — обе через контур подтверждения, со снимком вида в журнале и откатом. Формулы
отдельной операции не потребовали: значение с `=` пишется через `USER_ENTERED`. Структурные
операции (insert/delete/addSheet/copy/autoFill) вынесены в **2.5b**: им нужна другая модель
отката и адресация, устойчивая к сдвигу A1. **Непокрыто живьём:** запись по коду плана для
новых операций — проверяется только человеком (`delivery/LIVE-RUN.md`).

**Фаза 2.6 — подтверждение как подпись.** Запись идёт только по коду плана из превью
(`planId`, шесть hex): без кода `confirm_required`, с чужим — `plan_mismatch` с новым планом
в ответе, повтор — `plan_already_applied`, `force` без кода не принимается. Перед записью
ядро перечитывает ячейки и сверяет со значениями «до» (`stale_value`) — это же спасает
`appendRow` от затирания строки, занятой чужой сессией. D-16 и D-17 в DESIGN.md.

**Фаза 2 — MCP.** Слайсы 1–4 сделаны: схема операций (zod), `policy/` с 20 машинными
правилами, аудит-журнал, откат, stdio-сервер с шестью инструментами
(`gc_targets`, `gc_read`, `gc_search`, `gc_scan`, `gc_apply`, `gc_undo`), ресурс
`policy://rules`, HTML-превью файлом, сборка `dist/`.

**Контур.** ① Delivery (delivery@1.84) + ② CQG (cqg@2.32): 23 хука pre-commit, 2 на
pre-push, `quality.yml` из 8 шагов, `contour_doctor` DEAD 0, мутационный гейт **63%** при
пороге 50 (замер 2026-09-02). ③ OKF не развёрнут — момент после устоявшихся модулей.

## Открытые хвосты

- **Пишущая половина 2.5a не проверена живьём.** Самое важное из этого списка: `formatCells`
  и `findReplace` пишут по коду плана, ведут журнал и откатываются — но только в тестах.
  Живой прогон возможен лишь с человеком: код плана обязан произнести он
  (`delivery/LIVE-RUN.md`). Чек-лист в архиве поставки, `live-run.md`.
- **Вход раз в неделю.** Приложение в Cloud Console в статусе Testing → Google обрубает
  refresh-токен через 7 дней. Ядро предупреждает с шестого дня (`gc_targets` → warnings).
  Снимается публикацией приложения — владелец решил делать её **в самом конце**.
- **CLI пишет мимо журнала.** `src/cli/manual.ts` зовёт `upsertRow` без `journal` и
  `journalSource`: из `gc upsert --write` не работают ни аудит, ни `write.plan-once`. Код
  плана передавать научился (`--confirm=<код>`). Чинится с CLI-фасадом фазы 3.
- **`policy://rules` отдаёт только прозу** `rules.md`; машинные правила (26 штук) через
  ресурс не видны. Решить: отдавать `rules.json` вторым ресурсом или не рассчитывать.
- **`format` тела колонки живьём не подтверждён** — на таблице владельца ни одна колонка
  тела не оформлена явно. Нужна такая колонка, иначе остаётся дырой.
- **Мутационные слабые места:** `map.ts` 57%, `values.ts` 61%, `find-replace-op.ts` 66%.
  Поднять порог до 70 — задача следующей фазы; сейчас порог падения 50.
- **Три копии старого движка** (`Gdocs-tabs editor` в SEO:Content, local-web-agent,
  Event Content System) переведены на новый OAuth-клиент, но без токенов. Вывод из
  эксплуатации — фаза 3. Файл клиента оттуда однажды спас доступ, см. выше.
- **Telegram-токен** из старого `config.json` не перевыпущен (фаза 0, за владельцем).
- Фазы дальше: **2.5b** структурные операции, 2.7 полный `gc_scan`, 3 CLI и итог дня,
  4 раннер и панель (React + Vite + TS + Tailwind, liquid glass из `local-web-agent`).

## Gotchas — то, что уже стоило времени

- **Гейты судят, а не украшают.** `delivery_check` дважды не пустил коммит: объявленные
  права без `.claude/settings.json`, решение без цены. Формат решений строгий: «выбрал X
  **вместо** Y — потому что Z», иначе гейт отклонит.
- **Живой прогон находит то, чего не видят юнит-тесты.** Прогон в другом клиенте дал
  шесть настоящих дефектов; смена аккаунта — ещё три. Оба раза причина одна: тесты
  вызывали обработчик напрямую, а дефект жил на шве.
- **`npm run build` без `rm -rf dist/policy`** вкладывал папку в папку, и собранный сервер
  отдавал 14 правил из 17 при «успешной» сборке. Оракул S10 в smoke это ловит.
- **«Ничего не изменилось» ядро обязано называть само.** `dryRun:false` на «значение уже
  такое» возвращал `ok` и не писал журнал — человек читал «готово» при пустом факте. Теперь
  статус `no_change`. Тот же класс: `nothing_to_undo` без причины, и модель дописала
  «журнал пуст», когда все четыре записи были просто откачены.
- **Обещание реестра исполняет механизм, а не память модели.** `gc_targets` отдавал «лист
  по умолчанию: Проекты», а `gc_read`/`gc_apply` без явного `sheet` уходили на первый лист
  книги («Инструкция»). Порядок старшинства: аргумент вызова → реестр → эвристика.
- **Тест может закреплять дефект.** Property-тест «upsert идемпотентен» требовал `ok` от
  записи, которая ничего не меняет, — то есть охранял ровно то поведение, что оказалось
  дефектом. Оракул проверять на «краснеет ли он без правки».
- **Версия файла Drive растёт и от собственной записи** (16 → 17 → 18 за цикл). Откат
  сверяет НАШИ ячейки, а не версию, иначе отказывал всегда.
- **`prompt=consent` без `select_account`** — Google не показывает выбор аккаунта, и вход
  уходит не туда. Оба значения обязательны, на это есть тест.
- **Поиск по Drive не видит всего, что читается по ID.** Файл, расшаренный ссылкой и не
  открытый в Drive, отсутствует в `files.list` при любом запросе. `gc_search` всегда
  печатает `incompleteBecause`.
- **macOS даёт bash 3.2:** ни `mapfile`, ни ассоциативных массивов — `check_grep_gate_ts.sh`
  написан портируемо.
- **Живой прогон вторым агентом находит то, чего не видят тесты.** 2026-09-02 — шесть
  дефектов против 190 тестов; 2026-09-03 — блокер и три дефекта против 266. Правила и три
  роли: `delivery/LIVE-RUN.md`. Главное условие — у тестера свой процесс сервера: своё
  соединение агента может быть СТАРШЕ правки.
- **Новый файл на языке вне масок гейтов — ложь контура.** `scripts/probe_mcp.mjs` не
  видел ни один гейт (маска длины была `*.ts *.py`), CI на main покраснел на
  `contour_doctor`: DEAD 1. Маски задаются env в `.pre-commit-config.yaml`
  (`LINT_LENGTH_GLOBS`), доктор это и проверяет — добавил `*.mjs`.
- **Коммит проверять по `git log`, а не по числу строк «Passed»**: хуки печатают успех
  своих шагов, а сам коммит при этом может не пройти.
- Секреты только в `~/.gconnect/<account>/` с правами 600. В `.gitignore` есть маска
  `client_secret*.json` — файл клиента не должен оказаться в репозитории под любым именем.

## Команды

```bash
npm run build            # tsc + копирование policy в dist (нужно перед запуском сервера)
npm test                 # 190 тестов
npm run gc -- status     # состояние профиля, возраст входа, предупреждения
npm run gc -- login      # вход в Google (откроет браузер, покажет выбор аккаунта)
npm run gc -- map <ссылка|alias> [лист]
npm run mcp              # сервер на stdio (клиенты запускают его сами по .mcp.json)

bash delivery/evals/smoke/run.sh    # продуктовые оракулы S1–S10
python3 scripts/delivery_check.py   # контур поставки
npx stryker run                     # мутационный гейт (~3 мин)
```
